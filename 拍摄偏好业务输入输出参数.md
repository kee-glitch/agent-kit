# 拍摄偏好生成：业务输入输出参数

> 文档用途：定义“根据剧本生成拍摄偏好”的业务输入、模型输出及校验口径。  
> 输入方式：自然语言优先，不限制为固定 JSON。  
> 输出方式：结构化 JSON。

## 1. 业务流程

```text
用户提交输入信息 → 系统识别剧本要素 → 生成整片拍摄偏好 → 校验输入回传、词表、数量与冲突 → 返回输入信息及偏好结果
```

## 2. 业务输入

### 2.1 输入原则

- 用户可使用自然语言、字段列表、表格、JSON，或多种格式混合输入。
- 不要求固定字段名、字段顺序或完整结构。
- 系统按语义识别剧本内容，不因格式不同而拒绝处理。
- 信息不完整时，系统应基于已有内容生成；只有真正影响判断的缺失项才进入输出警告。
- 输入只包含剧本及相关资产信息，不包含字幕设置。

### 2.2 可识别的业务信息

| 信息项 | 必填性 | 业务用途 | 输入示例 |
| --- | --- | --- | --- |
| 剧本标题 | 可选 | 辅助识别主题和内容类型 | 咖啡机 30 秒功能演示 |
| 故事梗概 | 建议提供 | 判断剧情核心、信息顺序和转化路径 | 从早晨赶时间切入，展示一键快速冲泡，最后引导购买 |
| 视频时长 | 建议提供 | 控制建议偏好总量 | 30 秒 |
| 前三秒画面 | 建议提供 | 判断开场的视觉钩子 | 咖啡液快速流入透明杯 |
| 前三秒台词 | 可选 | 判断开场表达方式和节奏 | No time for slow coffee? |
| 完整口播或台词 | 建议提供 | 判断口播、演示、情景或画外音何者主导 | 博主边操作边介绍冲泡步骤 |
| 结尾行动引导 | 可选 | 判断结尾的呈现方式 | 提醒用户点击购买 |
| 角色及立场 | 可选 | 判断人物可信度、表达姿态和出镜方式 | 忙碌的居家生活博主，以真实体验建立信任 |
| 叙事结构 | 可选 | 判断是否需要对比、过程演示或节奏变化 | PAS：痛点—放大—解决 |
| 人物资产 | 可选 | 判断人物类偏好的可执行性 | 一名 30 岁左右的女性生活博主 |
| 场景资产 | 建议提供 | 判断机位、景别和运镜是否可执行 | 现代美式厨房，单一拍摄场地 |
| 产品资产 | 建议提供 | 判断产品展示方式 | 黑色胶囊咖啡机，带银色品牌标识 |
| 道具资产 | 可选 | 判断操作、组合和场景展示方式 | 透明杯、咖啡豆、托盘 |
| 其他拍摄限制 | 可选 | 排除不可执行的偏好 | 只有一台固定相机，不拍室外场景 |

### 2.3 最低可用输入

至少需要提供能够判断视频主题或主要任务的信息，例如故事梗概、完整口播、主要画面描述中的任意一项。

如果输入只有标题且无法判断内容，系统仍可返回结果，但应降低选择数量，并在“警告”中提示判断依据不足。

### 2.4 自然语言输入示例

```text
这是一个 30 秒的咖啡机功能演示视频。开头三秒先展示咖啡快速流出的效果，随后由一名居家生活博主边操作边介绍一键冲泡步骤，结尾提醒观众点击购买。场景是现代美式厨房，产品是一台黑色胶囊咖啡机，桌面上有透明杯、咖啡豆和托盘。整条视频希望真实、清楚、有生活感。
```

## 3. 业务输出

### 3.1 输出结构

| 一级字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `输入信息` | object | 是 | 完整回传用户提供的“输入信息”对象 |
| `拍摄偏好` | object | 是 | 七类整片拍摄偏好 |
| `已选数量` | integer | 是 | 所有偏好数组的元素总数 |
| `风格摘要` | string | 是 | 整片主要拍摄风格，中文，不超过 50 字 |
| `选择理由` | array | 是 | 每个已选偏好的选择依据 |
| `警告` | array | 是 | 输入缺失、素材限制或潜在冲突；无问题时为空数组 |

### 3.2 输入信息回传规则

- “输入信息”必须作为输出 JSON 的第一个一级字段。
- 用户提供了“输入信息”对象时，完整保留其全部字段、层级、数组、布尔值、数字和文本。
- 不得在回传过程中删减字段、修改字段名、调整层级、总结内容、纠错或改写。
- 用户使用自然语言且没有明确对象时，输出 `{"输入信息":{"原始输入":"用户原始内容"}}`。
- 拍摄偏好的分析结果只能写入后续输出字段，不得回填或覆盖“输入信息”。

### 3.3 拍摄偏好对象

| 字段 | 类型 | 数量 | 合法取值 |
| --- | --- | ---: | --- |
| `产品表现` | string[] | 0～2 | 手部操作、开箱拆封、核心功能演示、使用步骤展示、使用效果展示、使用效果对比、材质细节、尺寸比例展示、360 度展示、便携收纳展示、多场景使用、配件组合展示、包装与清单展示、品牌标识特写 |
| `人物表现` | string[] | 0～2 | 正对镜头口播、侧面对话、采访问答、边走边说、边用边讲、情景演绎、自然抓拍、无台词表演、情绪反应、多人互动、局部出镜、画外音配合 |
| `景别构图` | string[] | 0～2 | 产品微距、人物特写、半身近景、全身中景、环境全景、中心构图、三分构图、对称构图、留白构图 |
| `视角机位` | string[] | 0～2 | 平视机位、第一视角、自拍视角、俯拍视角、低机位仰拍、过肩视角、桌面顶拍、贴地机位 |
| `运镜倾向` | string[] | 0～2 | 手持跟拍、平行跟拍、固定机位、缓慢推近、缓慢拉远、横向摇摄、升降运镜、环绕运镜 |
| `焦点表现` | string[] | 0～2 | 焦点转移、浅景深跟焦、主体锁焦、前景虚化、背景虚化 |
| `节奏表现` | string[] | 0～2 | 前后对比剪辑、慢动作、延时摄影、速度渐变、快速转场、甩镜转场、匹配剪辑、定格强调、分屏对比、一镜到底 |

### 3.4 选择理由对象

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `偏好` | string | 是 | 已选偏好名称，必须能在七类偏好数组中找到 |
| `理由` | string | 是 | 中文选择依据，应对应剧本内容或素材条件 |

每个已选偏好对应一条理由，不合并、不遗漏、不为未选偏好生成理由。

### 3.5 警告信息

“警告”为字符串数组，只记录会影响偏好判断或执行的问题，例如：

- 未提供明确时长，已按默认建议总量控制；
- 未提供人物资产，因此未选择人物出镜类偏好；
- 剧本要求多场景展示，但输入只提供一个场景；
- 两种偏好可能冲突，后续需分配到不同镜头。

警告不应包含普通解释、选择理由或无业务影响的字段缺失。

## 4. 输出校验规则

| 校验项 | 规则 | 不通过时的处理 |
| --- | --- | --- |
| JSON 格式 | 必须是可解析的合法 JSON | 重新生成输出 |
| 字段完整性 | 六个一级字段必须全部存在 | 补齐缺失字段 |
| 输入回传 | “输入信息”与用户提供的输入对象字段、层级和值一致 | 重新复制原始输入对象 |
| 类别完整性 | 七类偏好字段必须全部存在 | 缺失类别补为空数组 |
| 枚举合法性 | 偏好名称必须与词表完全一致 | 删除或重新映射非法值 |
| 单类数量 | 每类最多 2 项 | 按匹配优先级保留前 2 项 |
| 总数一致性 | “已选数量”等于七个数组元素总数 | 重新计算总数 |
| 理由完整性 | 每个已选偏好恰好对应一条理由 | 补充或去重 |
| 风格摘要 | 中文且不超过 50 字 | 压缩摘要 |
| 事实约束 | 不得加入输入中没有依据的事实 | 删除杜撰内容并重新判断 |

### 4.1 建议偏好总量

| 视频时长 | 建议总量 |
| --- | ---: |
| 15 秒 | 不超过 6 项 |
| 30 秒 | 不超过 8 项 |
| 45 秒 | 不超过 10 项 |
| 60 秒 | 不超过 10 项 |
| 90 秒 | 不超过 12 项 |
| 未提供时长 | 不超过 10 项 |

建议总量是模型选择约束，不是业务硬错误；如果剧本确有充分依据可以超过，但仍须遵守每类最多 2 项。

## 5. 标准输出示例

```json
{
  "输入信息": {
    "商品名称": "胶囊咖啡机",
    "目标人群": "工作日早晨时间紧张的咖啡消费者",
    "核心痛点": "传统冲泡步骤耗时",
    "成片目标时长": "30秒",
    "视频形式": "产品功能演示",
    "剧本标题": "30秒快速咖啡",
    "故事梗概": "从早晨赶时间切入，展示咖啡机一键快速冲泡，最后引导购买。",
    "前三秒钩子": {
      "画面动作": "咖啡液快速流入透明杯",
      "口播台词": "No time for slow coffee?"
    },
    "完整口播文案": "No time for slow coffee? Make your morning cup with one touch. Tap to get yours.",
    "视觉资产": {
      "人物": {"名称": "居家生活博主", "描述": "在厨房中自然演示产品"},
      "场景": {"名称": "现代美式厨房", "描述": "带桌面的单一室内拍摄场景"},
      "产品": {"名称": "胶囊咖啡机", "描述": "黑色机身，带银色品牌标识"}
    }
  },
  "拍摄偏好": {
    "产品表现": ["核心功能演示", "手部操作"],
    "人物表现": ["边用边讲", "自然抓拍"],
    "景别构图": ["产品微距"],
    "视角机位": ["桌面顶拍"],
    "运镜倾向": ["固定机位"],
    "焦点表现": ["主体锁焦"],
    "节奏表现": []
  },
  "已选数量": 8,
  "风格摘要": "真实生活化的桌面操作演示，突出产品细节与使用过程",
  "选择理由": [
    {"偏好": "核心功能演示", "理由": "剧本核心任务是展示咖啡机一键快速冲泡。"},
    {"偏好": "手部操作", "理由": "剧本包含人物实际操作咖啡机的过程。"},
    {"偏好": "边用边讲", "理由": "人物需要在操作过程中介绍冲泡步骤。"},
    {"偏好": "自然抓拍", "理由": "剧本要求整体呈现真实、生活化的体验。"},
    {"偏好": "产品微距", "理由": "前三秒需要突出咖啡流出和产品使用效果。"},
    {"偏好": "桌面顶拍", "理由": "产品和道具均集中在厨房桌面，适合展示操作关系。"},
    {"偏好": "固定机位", "理由": "操作步骤需要清楚、稳定地呈现。"},
    {"偏好": "主体锁焦", "理由": "需要保证咖啡机及冲泡过程持续清晰。"}
  ],
  "警告": []
}
```

## 6. 空结果与低信息输入

如果输入无法支持任何可靠偏好，仍需返回完整结构：

```json
{
  "输入信息": {
    "原始输入": "制作一个产品视频"
  },
  "拍摄偏好": {
    "产品表现": [],
    "人物表现": [],
    "景别构图": [],
    "视角机位": [],
    "运镜倾向": [],
    "焦点表现": [],
    "节奏表现": []
  },
  "已选数量": 0,
  "风格摘要": "暂无法确定主要拍摄风格",
  "选择理由": [],
  "警告": ["输入信息不足，无法判断视频内容和主要拍摄任务。"]
}
```
