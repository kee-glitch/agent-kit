# 拍摄偏好规则

> 文档用途：统一“拍摄偏好”页面的产品逻辑、交互反馈和生成侧使用方式。  
> 适用阶段：拍摄偏好设置、剧本生成、分镜生成与分镜编辑。  
> 更新日期：2026-09-08

## 1. 规则摘要

1. 拍摄偏好是面向整条视频的“偏好池”，不是逐镜头配置。
2. 用户可以跨类别组合选择；每个类别最多选择 2 项，不设置全局硬上限。
3. 系统根据成片时长给出建议总量。超过建议值只提示，不阻止继续选择。
4. 用户选择表示“优先采用”，不表示每项必须出现，也不表示每个镜头都要使用。
5. 系统在生成分镜时按镜头任务择优分配，并在镜头层处理冲突。
6. 用户未选择的拍摄手法并非禁用项；为保证镜头完整、合理、可执行，系统可以自动补充。
7. 商品真实性、叙事目标和素材可执行性始终高于偏好覆盖率。

## 2. 功能定位

拍摄偏好用于让用户以较低的配置成本，表达整条视频希望呈现的内容、人物状态和画面语言。系统负责把整片偏好转换为适合不同镜头任务的具体配置，同时保持成片统一，避免机械堆叠。

| 情况 | 系统解释 |
| --- | --- |
| 用户已选择 | 生成时优先采用，但不保证一定使用 |
| 用户未选择 | 不代表禁止，必要时系统可补充 |
| 偏好未被采用 | 当前剧本、素材或镜头任务没有合适位置，不强行加入 |
| 多项偏好冲突 | 在具体镜头中择优保留，其余尝试分配到其他镜头 |

## 3. 偏好分类

页面按照“拍什么 → 谁来表现 → 怎么构图 → 从哪里看 → 怎么运动 → 怎么聚焦 → 怎么剪辑”的顺序展示。

### 3.1 产品表现

定义产品通过什么内容或证据被展示，可影响剧本和分镜。

- 手部操作、开箱拆封、核心功能演示、使用步骤展示
- 使用效果展示、使用效果对比、材质细节、尺寸比例展示
- 360 度展示、便携收纳展示、多场景使用、配件组合展示
- 包装与清单展示、品牌标识特写

### 3.2 人物表现

定义人物如何出镜和传递信息，可影响剧本和分镜。

- 正对镜头口播、侧面对话、采访问答、边走边说
- 边用边讲、情景演绎、自然抓拍、无台词表演
- 情绪反应、多人互动、局部出镜、画外音配合

### 3.3 景别构图

定义主体在画面中的范围与构图方式，主要用于分镜。

- 产品微距、人物特写、半身近景、全身中景、环境全景
- 中心构图、三分构图、对称构图、留白构图

### 3.4 视角机位

定义相机观察主体的位置与角度，主要用于分镜。

- 平视机位、第一视角、自拍视角、俯拍视角
- 低机位仰拍、过肩视角、桌面顶拍、贴地机位

### 3.5 运镜倾向

定义镜头运动方式，主要用于分镜和视频生成。

- 手持跟拍、平行跟拍、固定机位、缓慢推近
- 缓慢拉远、横向摇摄、升降运镜、环绕运镜

### 3.6 焦点表现

定义主体清晰度、景深和视觉注意力，主要用于分镜和视频生成。

- 焦点转移、浅景深跟焦、主体锁焦、前景虚化、背景虚化

### 3.7 节奏表现

定义时间处理、镜头衔接和剪辑节奏。结构性选项可影响剧本，其余主要用于分镜和后期。

- 前后对比剪辑、慢动作、延时摄影、速度渐变、快速转场
- 甩镜转场、匹配剪辑、定格强调、分屏对比、一镜到底

## 4. 页面交互规则

### 4.1 选择规则

| 规则项 | 规则 |
| --- | --- |
| 跨类别选择 | 支持 |
| 单类别上限 | 最多 2 项 |
| 全局硬上限 | 无 |
| 再次点击已选项 | 取消选择 |
| 达到单类别上限后新增 | 阻止新增，保留已有选择 |
| 超过时长建议总量 | 允许继续选择，仅显示软提示 |

### 4.2 单类别上限反馈

某类别已选 2 项后，用户再选择该类别第三项时：

- 不替换、不清除已有选择；
- 不新增第三项；
- 在偏好区域底部显示提示，并明确具体类别。

推荐文案：

> “{类别名称}”最多选择 2 项，请先取消一项。

### 4.3 时长建议总量

| 成片目标时长 | 建议最多选择 |
| --- | ---: |
| 15 秒 | 6 项 |
| 30 秒 | 8 项 |
| 45 秒 | 10 项 |
| 60 秒 | 10 项 |
| 90 秒 | 12 项 |
| 自定义时长 | 默认 12 项；后续可按预计镜头数动态计算 |

超过建议数量后：

- 高亮“已选 N 项”；
- 显示软提示，但不禁用其他未选项；
- 数量回到建议范围后，提示自动消失；
- 切换视频时长后立即按新时长重新判断，不清空已有选择。

推荐文案：

> 当前时长建议选择不超过 N 项；系统仍会按镜头择优采用，未采用的偏好不会强行加入。

### 4.4 页面说明与状态

固定说明：

> 为整条视频选择偏好，生成分镜时将按镜头择优分配。

规则说明：

> 可跨类别组合，同类最多选择 2 项；系统会在生成分镜时处理镜头级冲突，每个镜头只采用一种主要运镜。

状态要求：

- 实时显示全部类别合计的“已选 N 项”。
- 选中态、未选态和不可新增状态应明确区分。
- 错误提示优先展示单类别上限问题；软提示只用于建议总量超限。
- 保存后再次进入页面，应恢复用户已保存的选择。

## 5. 剧本与分镜的使用边界

### 5.1 生成剧本

产品表现、人物表现，以及具有叙事结构含义的节奏表现（如前后对比、分屏对比、一镜到底）可以影响内容结构、人物行为和表达方式。

景别、机位、运镜和焦点原则上不改变核心文案事实，只作为后续视觉执行参考。

### 5.2 生成分镜

全部偏好均可参与分镜生成。系统按镜头任务决定是否采用，并把最终采用结果保存到具体镜头。

### 5.3 分镜编辑

进入故事面板后，用户可以修改单个镜头的最终分配结果。镜头级修改不反向改变整片偏好池，除非用户明确选择“应用到整片”。

## 6. 单镜头分配规则

系统先识别镜头的叙事任务，再从整片偏好池中匹配合适选项。

| 类别 | 单镜头建议数量 |
| --- | ---: |
| 产品表现 | 0～1 项 |
| 人物表现 | 0～1 项 |
| 景别构图 | 1 项 |
| 视角机位 | 1 项 |
| 运镜倾向 | 1 项主要运镜 |
| 焦点表现 | 0～1 项 |
| 节奏表现 | 0～1 项 |

整片可以选择同类 2 项，是为了让不同镜头分别使用；不代表同一镜头要同时叠加两项。

示例：

```text
镜头任务：展示产品快速冲泡过程
产品表现：核心功能演示
人物表现：边用边讲
景别构图：产品微距
视角机位：桌面顶拍
运镜倾向：固定机位
焦点表现：主体锁焦
节奏表现：无
```

## 7. 镜头级冲突处理

整片偏好选择阶段不进行硬性冲突拦截。只有当多个偏好准备分配到同一镜头时，才判断冲突。

### 7.1 常见冲突

- 固定机位不能与手持跟拍、平行跟拍、缓慢推近、缓慢拉远、横向摇摄、升降运镜或环绕运镜同时作为同一镜头的主要运镜。
- 缓慢推近与缓慢拉远不能同时作为同一镜头的主要运镜。
- 桌面顶拍与低机位仰拍不能同时用于同一镜头。
- 俯拍视角与低机位仰拍不能同时用于同一镜头。
- 一镜到底不能与快速转场、甩镜转场、匹配剪辑或分屏对比同时用于同一连续镜头段。

### 7.2 处理顺序

1. 保留最符合当前镜头任务的选项。
2. 尝试把其余偏好分配到后续合适镜头。
3. 如果整条视频都没有合适位置，则允许不采用。
4. 不为覆盖所有偏好而新增无意义镜头或破坏叙事节奏。

## 8. 匹配优先级

```text
镜头叙事任务
> 商品与营销信息准确性
> 素材条件与可执行性
> 产品表现 / 人物表现
> 景别构图 / 视角机位
> 运镜倾向 / 焦点表现
> 节奏表现
> 偏好覆盖率
```

当用户偏好与商品真实性、素材条件或叙事任务冲突时，优先保证内容准确和镜头可执行。

## 9. 整片一致性规则

- 选择一个主要视角，作为整片视觉基准。
- 选择一个主要运镜，作为整片运动基调。
- 建议 60%～70% 的镜头保持相近的画面语言。
- 特殊运镜优先用于前三秒钩子、重点卖点和结果证明。
- 产品外观、人物、服装、场景和光线由核心资产统一约束。
- 同一种转场或强调效果不应高频重复。
- 短视频应减少手法数量并突出单一视觉方向；长视频可以覆盖更多偏好。

## 10. 自动推荐规则

用户没有选择偏好时，系统可以根据视频形式提供推荐，但推荐项不得自动显示为用户已选。只有用户主动确认或点击后，才计入“已选 N 项”。

| 视频形式 | 推荐倾向 |
| --- | --- |
| UGC 真人口播 | 半身近景、自拍视角、手持跟拍、正对镜头口播 |
| 产品功能演示 | 产品微距、桌面顶拍、固定机位、核心功能演示、主体锁焦 |
| 开箱体验 | 半身近景、桌面顶拍、固定机位、开箱拆封、自然抓拍 |
| 对比测评 | 使用效果对比、前后对比剪辑、定格强调、主体锁焦 |
| 品牌氛围片 | 环境全景、缓慢推近、浅景深跟焦、匹配剪辑 |

## 11. 异常与边界情况

| 场景 | 处理方式 |
| --- | --- |
| 用户未选任何偏好 | 允许继续；系统按视频类型和镜头任务自动生成 |
| 用户只选 1 项 | 允许继续；其余必要配置由系统补充 |
| 用户超过时长建议总量 | 允许继续，显示软提示 |
| 用户尝试选择同类第 3 项 | 阻止新增，显示类别上限提示 |
| 切换时长后原选择超出建议 | 保留全部选择，重新显示软提示 |
| 偏好与素材条件冲突 | 以素材可执行性为准，可不采用该偏好 |
| 偏好无法分配到任何镜头 | 不强行加入；生成结果中允许缺席 |
| 返回上一步修改剧本或资产 | 重新评估偏好分配，不自动清空整片偏好 |

## 12. 验收口径

### 12.1 选择与取消

- 任一未选项可被选择，并立即更新选中态和总数。
- 再次点击已选项可取消选择，并立即更新总数。
- 不同类别之间互不占用名额。

### 12.2 上限控制

- 每个类别可正常选择 2 项。
- 尝试选择同类第 3 项时，原有 2 项保持不变。
- 提示文案包含正确的类别名称。

### 12.3 时长软提示

- 不同时长使用对应的建议数量。
- 超过建议数量时不阻止继续选择或进入下一步。
- 数量恢复到建议范围后，软提示消失。
- 切换时长不清空选择。

### 12.4 生成结果

- 生成结果不要求覆盖全部用户偏好。
- 同一镜头只有一种主要运镜。
- 冲突偏好不会被强行分配到同一镜头。
- 未选择的必要手法可由系统补充。
- 分镜中采用的偏好可被用户逐镜头修改。

## 13. 完整处理流程

```text
用户选择整片拍摄偏好
        ↓
校验单类别最多 2 项
        ↓
根据成片时长计算软提示
        ↓
产品表现、人物表现及结构性节奏参与剧本生成
        ↓
完整偏好池参与分镜生成
        ↓
按镜头任务匹配偏好
        ↓
执行镜头级冲突检测
        ↓
检查整片一致性与偏好覆盖情况
        ↓
输出可编辑的分镜配置
```
