# 拍摄偏好逻辑规则

## 1. 功能定位

“拍摄偏好”是整条视频的偏好池，用于表达用户期望出现的画面和拍摄手法。

- 用户按完整成片选择偏好。
- 系统在生成分镜时，根据每个镜头的叙事任务择优分配。
- 用户选择的是“优先采用”，不是要求每个镜头强制使用。
- 用户未选择的手法不代表禁止使用；分镜缺少必要配置时，系统可以自动补充。
- 未被分配的偏好不应强行加入分镜。

## 2. 参数分类与顺序

页面按“拍什么 → 谁来表现 → 怎么构图 → 从哪里看 → 怎么运动 → 怎么聚焦 → 怎么剪辑”的顺序展示。

### 2.1 产品表现

决定产品通过什么内容和证据被展示，可参与剧本与分镜生成。

- 手部操作
- 开箱拆封
- 核心功能演示
- 使用步骤展示
- 使用效果展示
- 使用效果对比
- 材质细节
- 尺寸比例展示
- 360 度展示
- 便携收纳展示
- 多场景使用
- 配件组合展示
- 包装与清单展示
- 品牌标识特写

### 2.2 人物表现

决定人物用什么方式传递信息，可参与剧本与分镜生成。

- 正对镜头口播
- 侧面对话
- 采访回答
- 边走边说
- 边用边讲
- 情景演绎
- 自然抓拍
- 无台词表演
- 情绪反应
- 多人互动
- 局部出镜
- 画外音配合

### 2.3 景别构图

决定画面范围和主体在画面中的组织方式，主要用于分镜生成。

- 产品微距
- 人物特写
- 半身近景
- 全身中景
- 环境全景
- 中心构图
- 三分构图
- 对称构图
- 留白构图

### 2.4 视角机位

决定摄像机从什么位置观察主体，主要用于分镜生成。

- 平视机位
- 第一视角
- 自拍视角
- 俯拍视角
- 低机位仰拍
- 过肩视角
- 桌面顶拍
- 贴地机位

### 2.5 运镜倾向

决定拍摄过程中摄像机如何运动，主要用于分镜和视频生成。

- 手持跟拍
- 平行跟拍
- 固定机位
- 缓慢推近
- 缓慢拉远
- 横向摇摄
- 升降运镜
- 环绕运镜

### 2.6 焦点表现

决定主体清晰度、景深和视觉注意力的变化，主要用于分镜和视频生成。

- 焦点转移
- 浅景深跟焦
- 主体锁焦
- 前景虚化
- 背景虚化

### 2.7 节奏表现

决定时间处理、镜头衔接和剪辑节奏，结构性选项可以影响剧本，其余主要用于分镜与后期。

- 前后对比剪辑
- 慢动作
- 延时摄影
- 速度渐变
- 快速转场
- 甩镜转场
- 匹配剪辑
- 定格强调
- 分屏对比
- 一镜到底

## 3. 页面选择规则

### 3.1 选择数量

- 支持跨类别组合。
- 每个类别最多选择 2 项。
- 不设置全局硬上限。
- 超过当前时长的建议数量时显示软提示，但不阻止用户继续选择。

### 3.2 时长对应的建议总量

| 成片目标时长 | 建议最多选择 |
| --- | ---: |
| 15 秒 | 6 项 |
| 30 秒 | 8 项 |
| 45 秒 | 10 项 |
| 60 秒 | 10 项 |
| 90 秒 | 12 项 |
| 自定义时长 | 默认 12 项，后续可按预计镜头数动态计算 |

软提示文案：

> 当前时长建议选择不超过 N 项；系统仍会按镜头择优采用，未采用的偏好不会强行加入。

### 3.3 同类上限提示

当同一类别已经选择 2 项时，继续选择该类别的第三项应被阻止。

提示文案：

> “类别名称”最多选择 2 项，请先取消一项。

### 3.4 取消选择

- 用户可以随时取消已选偏好。
- 取消后应立即更新已选数量。
- 取消某项不应改变其他类别的选择。
- 如果取消后已经不再超过建议数量，应立即移除软提示。

## 4. 剧本与分镜的使用边界

### 4.1 生成剧本时

剧本模型只读取会影响内容结构和人物行为的偏好：

- 产品表现
- 人物表现
- 节奏表现中的结构性选项，例如“前后对比剪辑”和“一镜到底”

景别、机位、运镜、焦点等技术参数不应大量写入剧本文案，避免生成充满镜头术语但叙事不连贯的剧本。

### 4.2 生成分镜时

分镜模型读取全部七类偏好，并根据每个镜头的任务分配。

推荐处理顺序：

1. 判断镜头的叙事任务。
2. 判断镜头中是否包含产品、人物或环境。
3. 优先匹配用户已选的产品表现和人物表现。
4. 确定景别构图和视角机位。
5. 分配主要运镜和焦点表现。
6. 根据整片节奏分配剪辑或时间效果。
7. 检查镜头级冲突。
8. 检查整片视觉一致性。

## 5. 单镜头分配规则

每个镜头建议最多分配：

| 类型 | 单镜头建议数量 |
| --- | ---: |
| 产品表现 | 0～1 项 |
| 人物表现 | 0～1 项 |
| 景别构图 | 1 项景别，可附带 1 项构图 |
| 视角机位 | 1 项 |
| 运镜倾向 | 1 项 |
| 焦点表现 | 0～1 项 |
| 节奏表现 | 0～1 项 |

不是每个镜头都必须配置全部七类参数。镜头信息应以完成叙事任务为先。

示例：

```text
镜头任务：展示产品快速冲泡过程
产品表现：核心功能演示
人物表现：边用边讲
景别构图：产品微距
视角机位：桌面顶拍
运镜倾向：固定机位
焦点表现：主体锁焦
节奏表现：无
```

## 6. 镜头级冲突处理

整片偏好选择阶段不进行硬性冲突拦截。冲突只在系统把偏好分配到具体镜头时处理。

### 6.1 常见冲突

- 固定机位不能与手持跟拍、平行跟拍、推近、拉远、摇摄、升降或环绕同时用于同一镜头。
- 缓慢推近与缓慢拉远不能同时作为同一镜头的主要运镜。
- 桌面顶拍与低机位仰拍不能同时用于同一镜头。
- 俯拍视角与低机位仰拍不能同时用于同一镜头。
- 一镜到底不能与快速转场、甩镜转场、匹配剪辑或分屏对比同时应用于同一连续镜头段。

### 6.2 处理策略

当两个用户偏好发生镜头级冲突时：

1. 保留更符合当前镜头任务的选项。
2. 将另一项分配到后续合适镜头。
3. 如果整条视频都没有合适位置，可以不采用该项。
4. 不应为了覆盖所有偏好而生成无意义镜头。

## 7. 匹配优先级

分镜分配优先级从高到低为：

```text
镜头叙事任务
> 商品与营销信息准确性
> 产品表现 / 人物表现
> 景别构图 / 视角机位
> 运镜倾向 / 焦点表现
> 节奏表现
> 偏好覆盖率
```

当用户偏好与商品真实性、素材条件或叙事任务冲突时，应优先保证内容准确和镜头可执行。

## 8. 整片一致性规则

- 选择一个主要视角作为整片视觉基准。
- 选择一个主要运镜作为整片运动基调。
- 建议 60%～70% 的镜头保持相近的画面语言。
- 特殊运镜优先用于前三秒钩子、重点卖点和结果证明。
- 产品外观、人物、服装、场景和光线应由核心资产统一约束。
- 同一种转场或强调效果不应被高频重复使用。
- 长视频可以覆盖更多偏好，短视频应减少手法数量并突出单一视觉方向。

## 9. 自动推荐规则

用户未选择拍摄偏好时，系统可以根据视频形式提供默认推荐，但不要自动显示为用户已选。

示例：

| 视频形式 | 推荐倾向 |
| --- | --- |
| UGC 真人口播 | 半身近景、自拍视角、手持跟拍、正对镜头口播 |
| 产品功能演示 | 产品微距、桌面顶拍、固定机位、核心功能演示、主体锁焦 |
| 开箱体验 | 半身近景、桌面顶拍、固定机位、开箱拆封、自然抓拍 |
| 生活方式短片 | 环境全景、平视机位、平行跟拍、多场景使用、自然抓拍 |
| 前后效果对比 | 半身近景、固定机位、前后对比剪辑、使用效果对比、情绪反应 |

## 10. 数据存储建议

前端只保存用户选择的整片级偏好，不提前保存系统推断出的镜头配置。

```json
{
  "shootingPreferences": {
    "scope": "video",
    "groups": {
      "productPerformance": ["核心功能演示", "使用效果对比"],
      "personPerformance": ["边用边讲", "情绪反应"],
      "shotComposition": ["半身近景", "产品微距"],
      "cameraAngle": ["平视机位", "桌面顶拍"],
      "cameraMovement": ["固定机位", "缓慢推近"],
      "focusTreatment": ["主体锁焦"],
      "rhythmTreatment": ["前后对比剪辑", "匹配剪辑"]
    }
  }
}
```

生成分镜后，每个镜头保存系统最终分配结果：

```json
{
  "shotId": "shot-04",
  "purpose": "展示产品快速冲泡过程",
  "assignedPreferences": {
    "productPerformance": "核心功能演示",
    "personPerformance": "边用边讲",
    "shotComposition": "产品微距",
    "cameraAngle": "桌面顶拍",
    "cameraMovement": "固定机位",
    "focusTreatment": "主体锁焦",
    "rhythmTreatment": null
  }
}
```

## 11. 状态与文案

页面固定说明：

> 为整条视频选择偏好，生成分镜时将按镜头择优分配。

规则说明：

> 可跨类别组合，同类最多选择 2 项；系统会在生成分镜时处理镜头级冲突，每个镜头只采用一种主要运镜。

状态要求：

- 实时显示“已选 N 项”。
- 超过时长建议数量时高亮数量并显示软提示。
- 同类达到上限时只阻止新增，不清除已有选择。
- 切换时长时重新计算软提示，不清空用户选择。
- 进入分镜后允许用户修改单个镜头的最终分配结果。

## 12. 完整处理流程

```text
用户选择整片拍摄偏好
        ↓
校验同类最多 2 项
        ↓
根据成片时长计算软提示
        ↓
产品表现、人物表现和结构性节奏参与剧本生成
        ↓
完整偏好池参与分镜生成
        ↓
按镜头任务匹配偏好
        ↓
执行镜头级冲突检测
        ↓
检查整片一致性和偏好覆盖率
        ↓
输出可编辑的分镜配置
```

