# 图片分析接口

当前按产品原型需求运行模拟流程：上传完成后自动显示 1.8 秒分析中动画，再填入示例评分、标题和说明，无重新分析按钮。评分组件暂不调用真实接口。`src/image-analysis.js` 保留为后续真实接入适配器。

在本地 `.env.local` 配置 `VITE_IMAGE_ANALYSIS_URL` 为应用后端的图片分析接口地址，重启 Vite。该地址不是模型供应商地址；模型密钥应保存在后端，不能放入前端环境变量。

请求：`POST`，`multipart/form-data`，图片字段名 `image`。后端返回 JSON：

```json
{
  "qualityScore": 90,
  "title": "商品正面主图",
  "description": "依据实际图片生成的质量说明"
}
```

评分必须为 0–100 数字；标题和说明必须为非空字符串。前端包含加载、60 秒超时、错误重试和卸载取消请求逻辑。

当前仓库没有实际 AI 服务或密钥配置。上述接口配置仅供后续真实接入参考，当前模拟流程无需配置。
